# Python-only dependencies for CCRO-NUBS stage one.
# CMake, Eigen3 and the C++ compiler are supplied by the py310 conda environment.
numpy>=1.24
scipy>=1.10
pyyaml>=6.0
matplotlib>=3.7
pybind11>=2.10
pytest>=7.0
